{
  "name": "UserProfile",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "bio": {
      "type": "string"
    },
    "is_public": {
      "type": "boolean",
      "default": true
    },
    "show_library": {
      "type": "boolean",
      "default": true
    },
    "show_stats": {
      "type": "boolean",
      "default": true
    },
    "show_reviews": {
      "type": "boolean",
      "default": true
    },
    "tc_agreed": {
      "type": "boolean",
      "default": false
    },
    "tc_agreed_date": {
      "type": "string"
    },
    "age_verified": {
      "type": "boolean",
      "default": false
    },
    "age_filter": {
      "type": "string",
      "enum": [
        "all",
        "teen",
        "adult"
      ],
      "default": "all"
    },
    "blacklisted_themes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "onboarding_complete": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "user_email"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "data.is_public": true
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}