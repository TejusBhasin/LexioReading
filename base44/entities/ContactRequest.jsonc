{
  "name": "ContactRequest",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "subject": {
      "type": "string"
    },
    "message": {
      "type": "string"
    },
    "recipient": {
      "type": "string",
      "enum": [
        "lexio_admins",
        "school"
      ],
      "default": "lexio_admins",
      "description": "Who the request is directed to: 'lexio_admins' or 'school'"
    },
    "school_id": {
      "type": "string",
      "description": "School ID when recipient is 'school'"
    },
    "school_name": {
      "type": "string",
      "description": "School name when recipient is 'school'"
    },
    "status": {
      "type": "string",
      "enum": [
        "open",
        "replied",
        "closed"
      ],
      "default": "open"
    },
    "admin_reply": {
      "type": "string"
    }
  },
  "required": [
    "user_email",
    "subject",
    "message"
  ],
  "rls": {
    "create": {
      "data.user_email": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}