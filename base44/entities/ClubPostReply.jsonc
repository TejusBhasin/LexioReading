{
  "name": "ClubPostReply",
  "type": "object",
  "properties": {
    "post_id": {
      "type": "string"
    },
    "club_id": {
      "type": "string"
    },
    "user_email": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "content": {
      "type": "string"
    }
  },
  "required": [
    "post_id",
    "club_id",
    "user_email",
    "content"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.club_id": {
            "$in": [
              {
                "entity": "ReadingClub",
                "field": "id",
                "query": {
                  "data.member_emails": "{{user.email}}"
                }
              }
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.club_id": {
            "$in": [
              {
                "entity": "ReadingClub",
                "field": "id",
                "query": {
                  "data.member_emails": "{{user.email}}"
                }
              }
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.club_id": {
            "$in": [
              {
                "entity": "ReadingClub",
                "field": "id",
                "query": {
                  "created_by_id": "{{user.id}}"
                }
              }
            ]
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}