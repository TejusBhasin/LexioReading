/**
 * Centralized app configuration.
 *
 * All app-specific values (App Store URL, branding, etc.) live here so
 * onboarding and profile components remain generic and reusable
 * across any project without modification.
 *
 * To adapt for a different app, change only the values in this file.
 */
export const APP_CONFIG = {
  /** Full URL to the app's App Store listing. */
  appStoreUrl: 'https://apps.apple.com/us/app/lexio-reading/id6781776997',
};